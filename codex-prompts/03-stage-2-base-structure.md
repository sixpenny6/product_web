# 03 - Stage 2: Base Structure and Layout

执行阶段 2：基础结构和 Layout。

目标：

- 根据 `05_前端功能模块与组件清单.md` 创建基础目录；
- 添加：
  - `src/components/layout`
  - `src/components/product`
  - `src/components/viewer3d`
  - `src/components/forms`
  - `src/components/ui`
  - `src/lib`
  - `src/types`
- 创建 `AppHeader`、`Footer`、基础 layout；
- 设置全局深色背景；
- 顶部导航先用占位数据。

限制：

- 不做复杂 UI；
- 不做业务逻辑；
- 不做 CMS；
- 不做 3D；
- 不做表单后端。

完成后运行或说明：

- `npm run dev`
- `npm run lint`
