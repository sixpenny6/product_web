# 01 - Stage 0: Environment Check

执行阶段 0：环境检查。

本阶段不要创建文件，不要修改代码。

请检查这个项目需要的本地开发环境，并告诉我应该在 PowerShell 中运行哪些命令来确认：

1. Node.js 版本；
2. npm / pnpm 是否可用；
3. Git 是否可用；
4. 当前目录是否正确；
5. 是否已经初始化 git；
6. 是否已经放入 `docs/tubular_motor_website_docs`；
7. 是否已经放入 `AGENTS.md`。

请输出检查命令和判断标准。
